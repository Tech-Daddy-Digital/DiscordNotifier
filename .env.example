# Discord application credentials
# Create these at https://discord.com/developers/applications
DISCORD_TOKEN=replace-with-your-bot-token
DISCORD_CLIENT_ID=123456789012345678
DISCORD_CLIENT_SECRET=replace-with-your-oauth-client-secret
# Optional but recommended while testing: commands update instantly in one server.
DISCORD_GUILD_ID=234567890123456789
# Default Discord text channel for placeholder notifications.
DISCORD_NOTIFICATION_CHANNEL_ID=345678901234567890

# HTTP server
HTTP_HOST=0.0.0.0
HTTP_PORT=3000
# Public base URL users open in a browser. Discord OAuth callback must be:
#   https://your-domain.example/auth/callback
WEB_BASE_URL=http://localhost:3000
# Generate with: openssl rand -base64 48
SESSION_SECRET=replace-with-at-least-32-random-characters
# SQLite database path. Docker Compose persists ./data in a named volume.
DATABASE_PATH=./data/pulsedaddy.sqlite

# Optional bearer token for POST /notifications/placeholder and
# POST /notifications/mock/youtube-live.
# If set, callers must send: Authorization: Bearer <this value>
NOTIFICATION_WEBHOOK_TOKEN=replace-with-a-random-webhook-token

# Logging: fatal, error, warn, info, debug, trace, silent
LOG_LEVEL=info

# Set true to send a startup message to DISCORD_NOTIFICATION_CHANNEL_ID when the bot connects.
SEND_STARTUP_PLACEHOLDER_NOTIFICATION=false
